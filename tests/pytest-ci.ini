[pytest]
testpaths = tests
addopts = -v --tb=long
    --splunk_type=external
    --splunk_host=splunk
    --junitxml=/home/circleci/test-results/test.xml
    --splunk_password=Changed@11
    -n 10
junit_family=xunit2
filterwarnings =
    ignore::DeprecationWarning
